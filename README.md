/* 1. Give your content breathing room (Whitespace) */
section {
  padding: 80px 24px; /* Generous top/bottom padding makes it feel premium */
  max-width: 1200px;
  margin: 0 auto;
}

/* 2. Professional Typography Scale */
h1 {
  font-size: 2.5rem;
  font-weight: 700;
  letter-spacing: -0.02em; /* Tighter tracking on big headers looks modern */
  color: #0F172A;
}

p {
  font-size: 1.1rem;
  line-height: 1.7; /* Standard web text is too cramped; 1.7 is perfect for readability */
  color: #334155; /* Avoid pure black text on white backgrounds */
}

/* 3. Subtle, Modern Cards for Issues/Reports */
.report-card {
  background: #ffffff;
  border: 1px solid #E2E8F0; /* Soft border instead of a heavy dark shadow */
  border-radius: 8px;
  padding: 24px;
  box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.05); /* Very subtle depth */
}

